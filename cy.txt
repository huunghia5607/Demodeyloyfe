git clone https://github.com/huunghia5607/Demodeyloyfe.git .


Sao chép file TrangCaNhan.html và các thư mục liên quan vào thư mục Demodeyloyfe đang mở trong VS Code.

git add .
git commit -m "feat: initial commit of TrangCaNhan.html and associated files"

git push origin main

mkdir -p .github/workflows

new-item -Path .github/workflows/deploy.yml -Force

Dán Nội dung sau vào deploy.yml: (Đảm bảo đã thêm các Secrets FTP vào Repo GitHub trước đó)

YAML

# Tên của quy trình CI/CD
name: Deploy TrangCaNhan WebFE via FTP

on:
  push:
    branches:
      - main # Kích hoạt khi có thay đổi trên nhánh main

jobs:
  web-deploy:
    name: Deploy to Webhost
    runs-on: ubuntu-latest

    steps:
    # 1. Tải code từ GitHub
    - name: Checkout Code
      uses: actions/checkout@v4

    # 2. Triển khai bằng FTP
    - name: FTP Deploy to Somee Host
      uses: SamKirkland/FTP-Deploy-Action@v4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}

        # Thư mục gốc code trên máy cục bộ
        local-dir: ./ 

        # Thư mục đích chính xác trên host
       server-dir: /www.Nghia2k4.somee.com/PhamHuuNghia/


git add .
git commit -m "feat: add schedule update and deploy workflow"
git push origin main

//git add .github/workflows/deploy.yml
//git commit -m "ci: add GitHub Actions workflow for TrangCaNhan FTP deployment"


git push origin main


FTP_SERVER	Nghia2k4.somee.com
FTP_USERNAME	JKAMBROSE
FTP_PASSWORD	Mật khẩu control panel của bạn



git add .
git commit -m "fix: update 'Bài tập' text to 'ASSD'"
git push origin main



Commit file deploy.yml đã sửa:
Bash
git add .github/workflows/deploy.yml
git commit -m "fix: correct FTP deploy parameter from remote-dir to server-dir"
git push origin main

trường hợp mà đúng đường dẫn mà lỗi giao diện tùm lum thì ,ví dụ như trong flie yml chỗ server-dir: /www.Nghia2k4.somee.com/PhamHuuNghia/
mà file muốn sửa là trangcanhan.html nằm trong đúng đường dẫn đó mà bị lỗi giao diện 
thì đầu tiên sửa server-dir: /www.Nghia2k4.somee.com/ push lên lại sau đó ghi lại server-dir: /www.Nghia2k4.somee.com/PhamHuuNghia/ push lên lại 


muốn docker cập nhật cái mới:
Dừng và xóa container cũ:
docker-compose down
Build lại với code mới nhất và khởi động lại:
docker-compose up -d --build

môn kỹ năng giao tiếp:
lấy code mới nhất :
git pull origin main
đẩy lên :
 git add . hoặc git add "Kỹ năng giao tiếp.docx" 
git commit -m "fix: correct FTP deploy parameter from remote-dir to server-dir"
git push origin main
nếu xung đột thì gõ để thoát ra:
git merge --abort
 git fetch origin
git reset --hard origin/main

 git status
// thấy dòng sau là ok:
On branch main
Your branch is up to date with 'origin/main'.
sau đó:
git pull origin main
